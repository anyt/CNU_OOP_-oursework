{% extends 'base.html.twig' %}

{% block title %}{{ user.username }}{% endblock %}

{% block body %}
    <h1>{{ user.username }}</h1>

    {{ include('reservation/_list.html.twig', {'reservations': user.reservations}) }}


    <a class="btn btn-link" href="{{ path('app_user_index') }}">Повернутися до списку</a>

    {% if(user.id != app.user.id) %}
        {{ include('user/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
